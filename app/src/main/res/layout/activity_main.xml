<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#121212"
    android:fitsSystemWindows="true">

    <!-- Top Toolbar -->
    <LinearLayout
        android:id="@+id/header"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#1A1A1A"
        android:elevation="8dp"
        android:orientation="vertical">

        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="64dp"
            android:gravity="center_vertical"
            android:orientation="horizontal"
            android:paddingStart="16dp"
            android:paddingEnd="8dp">

            <ImageView
                android:id="@+id/ivLogo"
                android:layout_width="36dp"
                android:layout_height="36dp"
                android:src="@drawable/bruceirbymti"
                android:scaleType="centerInside" />

            <TextView
                android:id="@+id/tvHeaderTitle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_weight="1"
                android:layout_marginStart="12dp"
                android:text="BRUCE IR"
                android:textColor="#F44336"
                android:textSize="16sp"
                android:textStyle="bold" />

            <View
                android:id="@+id/vStatusDot"
                android:layout_width="10dp"
                android:layout_height="10dp"
                android:background="@android:color/holo_red_dark"
                android:layout_marginEnd="8dp" />

            <ImageButton
                android:id="@+id/btnSearch"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@android:drawable/ic_menu_search"
                app:tint="#FFFFFF" />

            <ImageButton
                android:id="@+id/btnSysConsole"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@android:drawable/stat_sys_data_bluetooth"
                app:tint="#4CAF50" />

            <ImageButton
                android:id="@+id/btnViewMode"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@android:drawable/ic_menu_sort_by_size"
                app:tint="#FFFFFF" />

            <ImageButton
                android:id="@+id/btnLock"
                android:layout_width="40dp"
                android:layout_height="40dp"
                android:background="?attr/selectableItemBackgroundBorderless"
                android:src="@android:drawable/ic_secure"
                app:tint="#FFFFFF" />
        </LinearLayout>

        <EditText
            android:id="@+id/etSearch"
            android:layout_width="match_parent"
            android:layout_height="50dp"
            android:background="#222222"
            android:hint="@string/search_hint"
            android:padding="10dp"
            android:textColor="#FFFFFF"
            android:singleLine="true"
            android:visibility="gone" />
    </LinearLayout>

    <!-- Section 1: REMOTES -->
    <FrameLayout
        android:id="@+id/sectionRemotes"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/header"
        android:layout_above="@id/bottomNav">
        <androidx.recyclerview.widget.RecyclerView
            android:id="@+id/recyclerView"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:padding="8dp" />
        <com.google.android.material.floatingactionbutton.FloatingActionButton
            android:id="@+id/fabAdd"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="bottom|end"
            android:layout_margin="24dp"
            android:src="@android:drawable/ic_input_add" />
    </FrameLayout>

    <!-- Section 2: CYBER -->
    <ScrollView
        android:id="@+id/sectionCyber"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/header"
        android:layout_above="@id/bottomNav"
        android:visibility="gone">
        <GridLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:columnCount="2"
            android:padding="16dp">
            <Button android:id="@+id/btnCyberTpms" android:text="TPMS TOOL" android:layout_width="0dp" android:layout_columnWeight="1" android:layout_margin="4dp" style="@style/Widget.Material3.Button.TonalButton"/>
            <Button android:id="@+id/btnCyberImmo" android:text="RFID/IMMO" android:layout_width="0dp" android:layout_columnWeight="1" android:layout_margin="4dp" style="@style/Widget.Material3.Button.TonalButton"/>
            <Button android:id="@+id/btnCyberSubGhz" android:text="SUB-GHZ" android:layout_width="0dp" android:layout_columnWeight="1" android:layout_margin="4dp" style="@style/Widget.Material3.Button.TonalButton"/>
            <Button android:id="@+id/btnCyberBle" android:text="BLE SPAM" android:layout_width="0dp" android:layout_columnWeight="1" android:layout_margin="4dp" style="@style/Widget.Material3.Button.TonalButton"/>
            <Button android:id="@+id/btnCyberC2" android:text="C2 BRIDGE" android:layout_width="0dp" android:layout_columnWeight="1" android:layout_margin="4dp" style="@style/Widget.Material3.Button.TonalButton"/>
            <Button android:id="@+id/btnCyberWps" android:text="WPS GRAB" android:layout_width="0dp" android:layout_columnWeight="1" android:layout_margin="4dp" style="@style/Widget.Material3.Button.TonalButton"/>
        </GridLayout>
    </ScrollView>

    <!-- Section 3: SYSTEM -->
    <ScrollView
        android:id="@+id/sectionSystem"
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:layout_below="@id/header"
        android:layout_above="@id/bottomNav"
        android:visibility="gone">
        <GridLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:columnCount="2"
            android:padding="16dp">
            <Button android:id="@+id/btnSysRemote" android:text="BRUCE REMOTE" android:layout_width="0dp" android:layout_columnWeight="1" android:layout_margin="4dp" style="@style/Widget.Material3.Button.TonalButton"/>
            <Button android:id="@+id/btnSysNav" android:text="NAVIGATOR" android:layout_width="0dp" android:layout_columnWeight="1" android:layout_margin="4dp"/>
            <Button android:id="@+id/btnSysNet" android:text="NET SCAN" android:layout_width="0dp" android:layout_columnWeight="1" android:layout_margin="4dp"/>
        </GridLayout>
    </ScrollView>

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNav"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:background="#1A1A1A"
        app:menu="@menu/bottom_nav_menu"
        app:itemIconTint="@color/white"
        app:itemTextColor="@color/white" />
</RelativeLayout>
